package com.teamarc.planit.configs;

import java.math.BigDecimal;

public class PlatformFeeConfig {
    public static final BigDecimal PLATFORM_FEE_RATE = BigDecimal.valueOf(0.02); // 2% Platform Charges
    public static final BigDecimal CANCELLATION_FEE_RATE = BigDecimal.valueOf(0.02); // 2% Cancellation fee
}
